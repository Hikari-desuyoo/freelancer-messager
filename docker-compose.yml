version: '3.3'
services:
  web:
    build: .
    stdin_open: true
    tty: true
    volumes:
      - .:/freelancer-messager
    command: bash -c "bin/setup && rails server -b 0.0.0.0 -p 5000"
    env_file:
      - .env
    ports:
      - "5000:5000"
    environment:
      TZ: Sao_Paulo/Sao_Paulo
    depends_on:
      - db

  db:
    image: postgres:13
    env_file:
      - .env
    environment:
      TZ: Sao_Paulo/Sao_Paulo